# nanobot 配置文件查找逻辑说明

## 核心逻辑

### 1. 创建配置（`nanobot onboard`）

```
总是在当前目录创建 config.json
```

**示例**：
```bash
cd /path/to/my-project
nanobot onboard
# 结果：创建 /path/to/my-project/config.json
```

```bash
cd ~/Documents
nanobot onboard
# 结果：创建 ~/Documents/config.json
```

---

### 2. 加载配置（`nanobot agent` 等命令）

```
优先级：
1. ./config.json (当前目录)
2. ~/.nanobot/config.json (全局配置)
3. 默认配置（如果都不存在）
```

**示例**：

#### 场景 A: 项目目录有配置
```bash
cd /path/to/nanobot
# 存在 ./config.json
nanobot agent -m "你好"
# 使用：./config.json
```

#### 场景 B: 项目目录无配置，但有全局配置
```bash
cd ~/Documents
# 不存在 ./config.json
# 存在 ~/.nanobot/config.json
nanobot agent -m "你好"
# 使用：~/.nanobot/config.json
```

#### 场景 C: 都不存在
```bash
cd ~/temp
# 不存在 ./config.json
# 不存在 ~/.nanobot/config.json
nanobot agent -m "你好"
# 使用：默认配置（会报错：No API key configured）
```

---

## 完整流程图

```
┌─────────────────────────────────────┐
│  nanobot onboard                    │
└─────────────────┬───────────────────┘
                  │
                  ▼
         在当前目录创建 config.json
         (无论是否已有全局配置)
                  │
                  ▼
         ┌────────────────────┐
         │ ./config.json      │
         │ ./workspace/       │
         └────────────────────┘


┌─────────────────────────────────────┐
│  nanobot agent (或其他命令)         │
└─────────────────┬───────────────────┘
                  │
                  ▼
         ./config.json 存在？
                  │
         ┌────────┴────────┐
         │                 │
        是                否
         │                 │
         ▼                 ▼
    使用项目配置    ~/.nanobot/config.json 存在？
                          │
                  ┌───────┴───────┐
                  │               │
                 是              否
                  │               │
                  ▼               ▼
             使用全局配置    使用默认配置
                              (报错)
```

---

## 代码实现

### `get_config_path()` - 用于创建配置

```python
def get_config_path() -> Path:
    """Get the default configuration file path."""
    # 总是返回当前目录的路径（用于创建新配置）
    return Path.cwd() / "config.json"
```

**用途**：
- `nanobot onboard` 创建配置时使用
- `save_config()` 保存配置时使用

---

### `load_config()` - 用于加载配置

```python
def load_config(config_path: Path | None = None) -> Config:
    if config_path:
        path = config_path
    else:
        # 查找顺序：项目 → 全局 → 默认
        project_config = Path.cwd() / "config.json"
        global_config = Path.home() / ".nanobot" / "config.json"
        
        if project_config.exists():
            path = project_config
        elif global_config.exists():
            path = global_config
        else:
            return Config()  # 默认配置
    
    # 加载配置文件...
```

**用途**：
- `nanobot agent` 等命令加载配置时使用
- 自动查找可用的配置文件

---

## 使用场景

### 场景 1: 项目独立配置

```bash
# 项目 A
cd ~/projects/project-a
nanobot onboard
# 创建 ~/projects/project-a/config.json
# 配置使用 qwen-max 模型

# 项目 B
cd ~/projects/project-b
nanobot onboard
# 创建 ~/projects/project-b/config.json
# 配置使用 deepseek-chat 模型

# 两个项目互不干扰
```

---

### 场景 2: 全局配置 + 项目覆盖

```bash
# 1. 设置全局配置
cd ~
nanobot onboard
# 创建 ~/.nanobot/config.json
# 配置默认模型

# 2. 在任意目录使用全局配置
cd ~/Documents
nanobot agent -m "你好"
# 使用 ~/.nanobot/config.json

# 3. 特定项目使用自己的配置
cd ~/projects/special-project
nanobot onboard
# 创建 ./config.json (覆盖全局配置)
nanobot agent -m "你好"
# 使用 ./config.json
```

---

### 场景 3: 团队协作

```bash
# 团队成员 A
git clone <repo>
cd project
nanobot onboard
# 创建 ./config.json
# 填入自己的 API 密钥

# 团队成员 B
git clone <repo>
cd project
nanobot onboard
# 创建 ./config.json
# 填入自己的 API 密钥

# .gitignore 中忽略 config.json
# 每个人使用自己的配置
```

---

## 对比表

| 操作 | 当前目录有 config.json | 全局有 config.json | 行为 |
|------|----------------------|-------------------|------|
| `nanobot onboard` | ✅ | ✅ | 在当前目录创建/覆盖 |
| `nanobot onboard` | ✅ | ❌ | 在当前目录创建/覆盖 |
| `nanobot onboard` | ❌ | ✅ | 在当前目录创建 |
| `nanobot onboard` | ❌ | ❌ | 在当前目录创建 |
| `nanobot agent` | ✅ | ✅ | 使用当前目录 |
| `nanobot agent` | ✅ | ❌ | 使用当前目录 |
| `nanobot agent` | ❌ | ✅ | 使用全局 |
| `nanobot agent` | ❌ | ❌ | 使用默认（报错） |

---

## 关键点总结

### ✅ 创建配置（onboard）

- **总是在当前目录创建**
- 不管是否有全局配置
- 不管当前目录是否已有配置（会提示覆盖）

### ✅ 加载配置（agent 等）

- **优先使用当前目录**
- 当前目录没有，回退到全局
- 都没有，使用默认配置

### ✅ 灵活性

- 可以项目独立配置
- 可以使用全局配置
- 可以混合使用

---

## 常见问题

### Q1: 我想让所有项目都使用全局配置怎么办？

**A**: 不要在项目目录运行 `nanobot onboard`，只在用户目录设置一次：

```bash
cd ~
nanobot onboard
# 编辑 ~/.nanobot/config.json

# 以后在任何项目目录运行都会使用全局配置
cd ~/projects/any-project
nanobot agent -m "你好"  # 使用 ~/.nanobot/config.json
```

---

### Q2: 我想让特定项目使用独立配置怎么办？

**A**: 在项目目录运行 `nanobot onboard`：

```bash
cd ~/projects/special-project
nanobot onboard
# 编辑 ./config.json

nanobot agent -m "你好"  # 使用 ./config.json
```

---

### Q3: 如何查看当前使用的是哪个配置文件？

**A**: 运行 `nanobot status` 或查看日志：

```bash
nanobot agent --logs -m "你好"
# 日志会显示加载的配置文件路径
```

---

### Q4: 我不小心在错误的目录运行了 onboard 怎么办？

**A**: 删除创建的文件：

```bash
del config.json
rmdir /s workspace
```

---

## 推荐实践

### 个人使用

```bash
# 设置全局配置
cd ~
nanobot onboard
# 编辑 ~/.nanobot/config.json

# 在任何地方使用
cd ~/Documents
nanobot agent -m "你好"
```

### 项目开发

```bash
# 每个项目独立配置
cd ~/projects/project-a
nanobot onboard
# 编辑 ./config.json

# 添加到 .gitignore
echo config.json >> .gitignore
echo workspace/ >> .gitignore
```

### 团队协作

```bash
# 提供配置模板
# config.example.json
{
  "agents": {
    "defaults": {
      "workspace": "./workspace",
      "model": "qwen-max"
    }
  },
  "providers": {
    "custom": {
      "apiKey": "YOUR_API_KEY_HERE",
      "apiBase": "https://dashscope.aliyuncs.com/compatible-mode/v1"
    }
  }
}

# 团队成员复制并填入自己的密钥
copy config.example.json config.json
# 编辑 config.json
```
